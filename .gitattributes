# Normalize all text files to LF in the repo, check out as LF
* text=auto eol=lf

# Binary files
*.png binary
*.ico binary
*.pfx binary
*.snk binary
*.nupkg binary

# Windows-specific files that need CRLF
*.sln text eol=crlf
